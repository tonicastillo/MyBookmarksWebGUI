services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
      - PORT=3003
      - PUBLIC_BASE_URL=${PUBLIC_BASE_URL:-}
    volumes:
      - bookmarks-data:/app/data
    restart: unless-stopped

volumes:
  bookmarks-data:
