# Server
PORT=3003

# Base URL para construir las URLs de imágenes en respuestas API.
# Vacío = URL relativa /images/{filename} (suficiente cuando frontend y backend
# se sirven desde el mismo origen, como en dev con Vite proxy).
PUBLIC_BASE_URL=https://bookmarks.tonicastillo.com/
