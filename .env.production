# Variables para producción (Dokploy / docker-compose).
# Este archivo es solo referencia: docker-compose.yml las consume vía ${VAR}.
# No es leído por el backend cuando NODE_ENV=production.

PORT=3003

# Dominio público desde el que se sirven las imágenes. Sin trailing slash.
PUBLIC_BASE_URL=https://bookmarks.tonicastillo.com
